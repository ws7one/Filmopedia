import { colors } from 'react-native-elements';

const goldColor = '#FFD700';
const white = '#FFFFFF';
const black = '#000000';

const theme = {
    ...colors,
    gold: goldColor,
    white,
    black
};

export default theme;
